"""This module contains `constants` re-usable across the app.

Instead of redefining these multiple times, which is prone to typo error
(e.g. incorrect capitalization / spelling), we chose to put them in this
module.
"""
